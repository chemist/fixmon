hosts:
  - salt
groups:
  - name: salt
    hosts:
      - salt
    triggers:
      - networkt
    checks:
      - network

checks:
  - name: network
    period: '* * * * *'
    version: 3
    sequrityName: aes
    privType: AES
    privAuth: AuthPriv
    authType: SHA
    authPass: helloallhello
    privPass: helloallhello
    type: snmp.network.interface

triggers:
  - name: networkt
    checks:
      - network
    description: тупо проверка имени
    result: not snmp.network.interface.eth0.name = "limbo-air" 



